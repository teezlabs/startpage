---
name: ZeroVer versioning preference
description: User always uses ZeroVer (0.x.y) versioning in package.json
type: feedback
---

Always use ZeroVer (0.x.y, e.g. `0.1.0`) for versioning — never start at `1.0.0`.

**Why:** User explicitly stated "0ver always".

**How to apply:** Any time a `package.json` is created, set version to `0.1.0` (or similar 0.x.y).
